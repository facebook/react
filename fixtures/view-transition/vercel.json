{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "outputDirectory": "build/static",
  "buildCommand": "",
  "ignoreCommand": "pwd; exit 1",
  "installCommand": "cd ../../ && yarn install && yarn build-for-vt-deploy && cd fixtures/view-transition && yarn install && yarn build",
  "env": {
    "BABEL_DISABLE_CACHE": "1",
    "DISABLE_ESLINT_PLUGIN": "true"
  },
  "functions": {
    "api/index.js": {
      "includeFiles": "src/**/*"
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index.js"
    }
  ]
}
