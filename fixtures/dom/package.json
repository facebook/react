{
  "name": "react-fixtures",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "classnames": "^2.2.5",
    "component-playground": "^2.0.0",
    "prop-types": "^15.5.10",
    "query-string": "^4.3.4",
    "react": "./stubs/react",
    "react-dom": "./stubs/react-dom",
    "semver": "^5.3.0"
  },
  "devDependencies": {
    "react-scripts": "0.9.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "prestart": "yarn run copy-local-react",
    "build": "react-scripts build",
    "prebuild": "yarn run copy-local-react",
    "copy-local-react": "cp ../../build/dist/{react,react-dom,react-dom-server}.development.js public/"
  }
}
