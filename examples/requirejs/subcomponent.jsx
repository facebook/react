/**
 * @jsx React.DOM
 */
/* global define */
define(['react'], function (React) {
  "use strict";

  return React.createClass({
    render : function () {
      return <div>Simple RequireJS Component</div>;
    }
  });
});
