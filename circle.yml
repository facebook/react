---
general:
  branches:
    ignore:
      - gh-pages

machine:
  timezone: America/Los_Angeles
  node:
    version: 4
  ruby:
    version: 2.2.3

dependencies:
  post:
    - npm install -g grunt
    - npm ls --depth=0

test:
  override: grunt jest:normal
