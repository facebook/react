---
general:
  branches:
    ignore:
      - gh-pages

machine:
  timezone: America/Los_Angeles
  node:
    version: 4
  ruby:
    version: 2.2.3

dependencies:
  post:
    - npm ls --depth=0

test:
  override:
    - ./node_modules/bin/grunt jest:normal
